Q1|D1|T1|1.5|You are building a customer support resolution agent using the Claude Agent SDK. The agent handles high-ambiguity requests like returns, billing disputes, and account issues. It has access to backend systems through MCP tools (get_customer, lookup_order, process_refund, escalate_to_human). Your target is 80%+ first-contact resolution while knowing when to escalate.

Production logs show that during short order-service outages, customers with valid orders are sometimes told that no matching order exists. The same tool response shape is currently used when a search completes with zero matches and when the order backend cannot be reached. What change would best improve the agent's recovery behavior?|A. Automatically retry every empty lookup_order response three times before telling customers that no matching orders were found. | B. Return successful empty results only for completed searches, and structured retryable errors when order lookup cannot access the backend. | C. Add a prompt instruction telling the agent to apologize and explain uncertainty whenever lookup_order returns empty results. | D. Treat all lookup_order failures as no-match outcomes, and escalate only if the customer disputes the result.|ANS:B|EXP:The root cause is that the tool's response shape is ambiguous ? the same structure is used for a successful empty search and for a backend access failure. The exam guide explicitly calls out this anti-pattern: distinguishing access failures from valid empty results is essential so the agent can choose the right recovery path (retry vs. clarification). Returning structured retryable errors for access failures while reserving the successful-empty response for genuine no-match results fixes the conflation.
Q2|D1|T1|1.3|You are building a customer support resolution agent using the Claude Agent SDK. The agent handles high-ambiguity requests like returns, billing disputes, and account issues. It has access to backend systems through MCP tools (get_customer, lookup_order, process_refund, escalate_to_human). Your target is 80%+ first-contact resolution while knowing when to escalate.

During a pilot, the coordinator verifies a customer, retrieves two recent orders, and then delegates separate billing-dispute and returns-policy checks. The delegated analyses frequently state that the customer ID or order status is unknown, even though those facts were available earlier in the coordinator's turn. The coordinator currently sends each delegated check only a brief task title such as "evaluate refund eligibility." What change would most reliably improve the delegated analyses?|A. Add few-shot examples instructing subagents to remember earlier coordinator turns during billing and return eligibility analysis. | B. Include the relevant verified customer facts, order details, and prior tool outputs directly in every delegated Task prompt. | C. Have subagents select customer and order records heuristically from names when required identifiers are missing from their prompts. | D. Increase the coordinator's conversation token budget so earlier verification and order lookup results remain available in session.|ANS:B|EXP:Subagents operate with isolated context and do not inherit the coordinator's conversation history automatically. Including the relevant verified customer facts, order details, and prior tool outputs directly in every delegated Task prompt gives each subagent the complete context it needs without requiring access to the coordinator's prior turns.
Q3|D4|T1|4.1|A research-assistant agent answers academic questions for graduate students using a provided document set. The current system prompt contains the rules: 'Don't make up citations. Don't speculate beyond what's grounded. Don't summarize papers you can't verify.' Quality review still finds ~7% of responses include fabricated citations to plausible-sounding but nonexistent papers. The team has tried strengthening the negative rules (all-caps, repetition, exception examples) with no measurable improvement. What change would most directly reduce fabrication?|A. Strengthen the negative rules further: convert each 'Don't' to 'NEVER' in all-caps, add 'Violations will be flagged and reviewed', and append a list of fabrication examples to avoid. | B. Replace the negative prohibitions with explicit positive criteria for what a valid citation requires ? 'Cite only papers present in the provided document set, by exact title, with a direct quote and page number; if the question cannot be answered from the document set, respond "I don't have grounded sources for this" and offer to search if a search tool is available.' | C. Add a self-check step where the agent re-reads its draft response and corrects any citation that doesn't trace to a specific document in the provided set before returning the final answer. | D. Reduce the model's sampling temperature to 0 and add `top_p: 0.1` to make the agent more deterministic and reduce creative output that produces fabricated citations.|ANS:B|EXP:The exam guide for subdomain 4.2 emphasizes explicit criteria ? specifying what valid output looks like, with verifiable conditions and fallback behavior ? over negative prohibitions. Negative rules ('don't fabricate') describe the failure mode but leave the agent unsupervised about what the success mode looks like; the model fills the unspecified space with plausible-looking content. Positive criteria define the citation contract (must be in the document set, must have quote and page, must explicitly fall back on absence) so the agent has a checkable target. This is the same principle behind the q-e83155ef pattern (long mixed-purpose system prompts drift; explicit role + criteria + few-shot examples stabilize).
Q4|D3|T2|3.2|Your CLAUDE.md has grown to over 400 lines containing coding standards, testing conventions, a detailed PR review checklist, deployment workflow instructions, and database migration procedures. You want Claude to always follow the coding standards and testing conventions, but only apply PR review, deployment, and migration guidance when you're actually performing those tasks. What's the most effective restructuring approach?|A. Split the CLAUDE.md into files in .claude/rules/ with path-specific glob patterns so each rule loads only for matching file types | B. Keep universal standards in CLAUDE.md and create Skills for task-specific workflows (PR reviews, deployments, migrations) with trigger keywords | C. Move all guidance into separate Skills files organized by workflow type, keeping only a brief project description in CLAUDE.md | D. Keep all content in CLAUDE.md but use @import syntax to organize it into separately maintained files by category|ANS:B|EXP:CLAUDE.md is for always-loaded universal standards (coding standards, test conventions) that apply to every interaction. Skills are designed for on-demand, task-specific workflows triggered by their description - exactly the right vehicle for PR reviews, deployments, and migrations that should activate only when the user is doing those tasks. Splitting along the always-on vs on-demand axis matches the documented trade-off.
Q5|D2|T3|2.4|A team built a `jira_search_issues` MCP tool that retrieves issues with status, priority, and assignee in a single call. The tool is registered and available to the agent, but the agent consistently uses the built-in `WebFetch` tool against Jira's web UI URL instead, requiring 3-4 turns to assemble what `jira_search_issues` returns in one. The current tool description is: `"Search Jira issues."` (verbatim). What is the correct fix per the exam guide's documented MCP integration patterns?|A. Remove `WebFetch` from the agent's available tools so the agent is forced to use `jira_search_issues`. | B. Enhance the `jira_search_issues` tool description with capability detail, example queries, structured output schema, and explicit guidance on when to prefer it over `WebFetch`. | C. Add a system prompt instruction: 'Always prefer `jira_search_issues` over `WebFetch` for Jira queries.' | D. Move `jira_search_issues` from `~/.claude.json` (user scope) to project-scoped `.mcp.json` so it has higher precedence in tool selection.|ANS:B|EXP:The exam guide's documented pattern for MCP integration is: 'Enhancing MCP tool descriptions to explain capabilities and outputs in detail, preventing the agent from preferring built-in tools (like Grep) over more capable MCP tools.' A minimal description like 'Search Jira issues.' gives the agent no basis for preferring `jira_search_issues` over `WebFetch` when both are available. Enhancing the description with what the tool returns, what query formats it accepts, and when it outperforms `WebFetch` (single-call vs. multi-turn HTML parsing) directly addresses the agent's tool-selection failure. Option A (removing WebFetch) prevents agent-initiated workarounds but also disables WebFetch for legitimate use cases ? it is a blunt capability restriction rather than the documented pattern of improving tool descriptions. Option C (system prompt instruction) is prompt-level guidance, which the exam guide distinguishes from structural fixes; it also duplicates information that belongs in the tool description. Option D (scoping to project .mcp.json) changes the configuration scope but does not affect tool selection logic ? the agent's preference for WebFetch is driven by the poor tool description, not by configuration scope.
Q6|D3|T2|3.2|Production logs reveal that the agent misinterprets data from your MCP tools: Unix timestamps from get_customer, ISO 8601 dates from lookup_order, and numeric status codes (1=pending, 2=shipped). Some tools are third-party MCP servers you cannot modify. What's the most maintainable approach to normalize data formats?|A. Use a PostToolUse hook to intercept tool results and apply formatting transformations before agent processing | B. Add detailed format documentation to your system prompt explaining each tool's data conventions | C. Modify tools you control to return human-readable formats; create wrapper tools for third-party tools | D. Create a normalize_data tool that the agent calls after each data retrieval to transform values|ANS:A|EXP:PostToolUse hooks intercept tool results before the agent processes them, making them the documented mechanism for normalizing heterogeneous formats (Unix timestamps, ISO 8601, numeric status codes) coming from different MCP servers, including third-party ones the team cannot modify.
Q7|D4|T1|4.2|Production logs show the agent sometimes selects get_customer when lookup_order would be more appropriate, particularly for ambiguous requests like "I need help with my recent purchase." You decide to add few-shot examples to your system prompt to improve tool selection. Which approach will most effectively address this issue?|A. Add 10-15 examples of clear, unambiguous requests that demonstrate correct tool selection for each tool's typical use cases. | B. Add examples grouped by tool-all get_customer scenarios together, then all lookup_order scenarios. | C. Add explicit "use when" and "do not use when" guidelines in each tool's description covering the ambiguous cases. | D. Add 4-6 examples targeting ambiguous scenarios, each showing reasoning for why one tool was chosen over plausible alternatives.|ANS:D|EXP:For tool-selection errors on ambiguous inputs, the recommended pattern is a small set (about 4-6) of targeted few-shot examples that focus specifically on ambiguous scenarios and show the reasoning for choosing one tool over plausible alternatives. This trains discrimination, not breadth.
Q8|D1|T1|1.3|You are building a customer support resolution agent using the Claude Agent SDK. The agent handles high-ambiguity requests like returns, billing disputes, and account issues. It has access to backend systems through MCP tools (get_customer, lookup_order, process_refund, escalate_to_human). Your target is 80%+ first-contact resolution while knowing when to escalate.

During rollout, the agent frequently spends its first several turns probing the support knowledge server with broad queries such as "what policies exist for billing disputes?" and "which return exceptions are available?" These exploratory calls consume context, sometimes miss relevant policy documents, and delay straightforward resolutions before any customer-specific action occurs. What design change would best improve the agent's visibility into available support information while preserving tools for customer-specific actions?|A. Copy the full return, billing, and escalation knowledge base into the system prompt for every agent session. | B. Build keyword rules that map phrases like refund, late, or angry to predefined policy documents before tool use. | C. Add a mandatory startup tool call that lists every policy document before the agent handles each customer request. | D. Expose the support policy index and knowledge-base catalog as MCP resources, while keeping tools for selected record actions.|ANS:D|EXP:MCP resources are designed to expose read-oriented catalogs and reference data?exactly what a policy index and knowledge-base catalog are. Exposing them as resources lets the agent inspect available content without making exploratory tool calls, reducing context consumption and latency before customer-specific actions. The exam guide identifies this pattern as the correct way to give agents visibility into available data without requiring exploratory tool calls.
Q9|D1|T1|1.5|You are building a customer support resolution agent using the Claude Agent SDK. The agent handles high-ambiguity requests like returns, billing disputes, and account issues. It has access to backend systems through MCP tools (get_customer, lookup_order, process_refund, escalate_to_human). Your target is 80%+ first-contact resolution while knowing when to escalate.

Production review finds several billing dispute cases where the agent correctly verifies the customer and order, then attempts to call process_refund for amounts above your policy limit. Company policy allows autonomous refunds up to $500, but larger refunds require human approval before any financial action occurs. What change would most reliably prevent policy violations while preserving autonomous handling for allowed refunds?|A. Scan the assistant's final text for refund amounts above $500 and create an audit alert after completion. | B. Update the process_refund tool description to warn that refunds above $500 are forbidden without human approval. | C. Strengthen the system prompt with a mandatory refund limit rule and examples showing escalation for larger amounts. | D. Add a tool call interception hook that blocks process_refund above $500 and returns guidance to call escalate_to_human.|ANS:D|EXP:A tool call interception hook provides deterministic enforcement: it blocks process_refund before the financial operation executes whenever the amount exceeds $500, and returns guidance to call escalate_to_human. This preserves autonomous handling for allowed refunds while guaranteeing policy compliance for larger amounts. The exam guide explicitly names this exact pattern ? blocking refunds exceeding $500 and redirecting to human escalation.
Q10|D2|T2|2.4|Your team is onboarding a shared GitHub MCP server that all engineers need access to for PR review automation. The server requires a GitHub API token for authentication. A junior engineer suggests each developer should configure the server in their personal `~/.claude.json` file. A senior engineer argues it should go in a project-level `.mcp.json` file at the repository root with `${GITHUB_TOKEN}` as the credential value. Which approach is correct, and why?|A. The junior engineer is correct: GitHub API tokens are personal credentials and should always be in user-level configuration to avoid token sharing. | B. The senior engineer is correct: `.mcp.json` at the project level is shared via version control so all engineers get the server automatically, while `${GITHUB_TOKEN}` expands from each developer's environment at runtime so the actual token is never committed. | C. Both approaches are valid: project-level vs user-level is a matter of preference, and either location will make the MCP server available to all team members. | D. Neither is fully correct: MCP servers should be configured via `claude mcp add` CLI command, which registers the server globally without requiring manual JSON editing.|ANS:B|EXP:The exam guide states: 'MCP server scoping: project-level (.mcp.json) for shared team tooling vs user-level (~/.claude.json) for personal/experimental servers' and 'Environment variable expansion in .mcp.json (e.g., ${GITHUB_TOKEN}) for credential management without committing secrets.' A GitHub MCP server used by all engineers for PR automation is exactly the shared team tooling case. Project-level `.mcp.json` is version-controlled and auto-discovered by all team members when they clone the repo. The `${GITHUB_TOKEN}` syntax expands at runtime from each developer's own environment (CI secret, local export), so the server config can be committed without committing credentials. User-level config (A) would require each developer to manually set it up individually, losing the shared-via-version-control benefit.
Q11|D1|T1|1.3|You are building a customer support resolution agent using the Claude Agent SDK. The agent handles high-ambiguity requests like returns, billing disputes, and account issues. It has access to backend systems through MCP tools (get_customer, lookup_order, process_refund, escalate_to_human). Your target is 80%+ first-contact resolution while knowing when to escalate.

During a pilot, the agent often spends its first two turns asking backend tools what refund policies, return reason codes, and escalation queues exist. These lists are read-only, change weekly, and help the agent choose the right customer-specific actions, but the extra calls increase latency and sometimes consume context before order lookup. Which MCP integration change best addresses this issue?|A. Instruct the agent to skip catalog discovery unless its response text mentions uncertainty about policy applicability. | B. Add a mandatory first-turn tool_choice forcing list_policy_catalog before any customer lookup, then let normal tool selection resume. | C. Expose the read-only policy, reason-code, and queue catalogs as MCP resources, while keeping customer-changing operations as tools. | D. Combine catalog listing and action execution into each backend tool so every call returns available policies and queues.|ANS:C|EXP:Task 2.4 states: 'MCP resources as a mechanism for exposing content catalogs (e.g., issue summaries, documentation hierarchies, database schemas) to reduce exploratory tool calls' and 'Exposing content catalogs as MCP resources to give agents visibility into available data without requiring exploratory tool calls.' Read-only policy/reason-code/queue catalogs are a natural fit for resources rather than tools.
Q12|D3|T2|3.6|Your team has written a bash script that invokes Claude Code as part of a CI pipeline step. When the script runs in your automated environment, the job hangs indefinitely after the Claude Code process starts and never produces output. Local runs complete successfully. What is the most likely cause, and what is the correct fix?|A. The CLAUDE.md file is not found in the CI environment; add a step to copy CLAUDE.md to the working directory before the Claude Code invocation. | B. Claude Code is waiting for interactive input because it was invoked without the `-p` (or `--print`) flag; add `-p` to run in non-interactive mode. | C. The CI environment lacks a display, preventing Claude Code from rendering its terminal UI; set the TERM environment variable to `dumb` to suppress the TUI. | D. The API key is not available in CI; the hang is a silent auth failure; add the ANTHROPIC_API_KEY to the CI environment secrets.|ANS:B|EXP:The exam guide states: 'The -p (or --print) flag for running Claude Code in non-interactive mode in automated pipelines' and 'Running Claude Code in CI with the -p flag to prevent interactive input hangs.' Without -p, Claude Code expects a human at the terminal and blocks waiting for input ? which produces exactly the described behavior: hangs in CI, works locally (where a human is present). The CLAUDE.md issue (A) would produce an error or degraded output, not a hang. A display issue (C) is not the documented cause of CI hangs in Claude Code; the -p flag is. Auth failure (D) typically produces an immediate error, not a silent hang.
Q13|D1|T1|1.3|The web search subagent returns results for only 3 of 5 requested source categories (competitor websites and industry reports succeeded, but news archives and social media feeds timed out). The document analysis subagent successfully processed all provided documents. The synthesis subagent must now produce a findings summary from this mixed-quality input. What's the most effective error propagation strategy?|A. Proceed with synthesis using only the successful sources, generating output without indicating which data was unavailable. | B. Have the synthesis subagent request the coordinator retry the timed-out sources with extended timeouts before proceeding, ensuring complete data coverage before synthesis begins. | C. Have the synthesis subagent return an error to the coordinator indicating incomplete upstream data, triggering a full retry or task failure. | D. Structure the synthesis output with coverage annotations indicating which findings are well-supported versus which topic areas have gaps due to unavailable sources.|ANS:D|EXP:The exam guide explicitly recommends structuring synthesis output with coverage annotations indicating which findings are well-supported versus which topic areas have gaps due to unavailable sources. This preserves transparency about completeness without silently dropping the missing categories or stalling the entire pipeline on partial failure.
Q14|D4|T1|4.2|Your AI tutor has a 2,800-token system prompt defining teaching methodology and adaptation rules. After 12 turns, the assistant starts ignoring proficiency levels. What is the most effective fix?|A. Inject reminders every 4-5 turns. | B. Replace verbose rules with few-shot examples demonstrating proficiency-level adaptation. | C. Place critical rules at the end of the system prompt. | D. Evaluate responses and regenerate if difficulty level mismatches.|ANS:B|EXP:When verbose rule-based instructions produce inconsistent behavior over many turns, replacing them with concrete few-shot examples that demonstrate the correct adaptation pattern is the most effective technique. Examples ground the desired behavior in observable model outputs rather than relying on the model to interpret abstract rules turn after turn.
Q15|D3|T2|3.6|After an initial automated review generates 12 findings, a developer pushes new commits to address the issues. When the review runs again, it produces 8 findings-but developers report that 5 duplicate earlier comments on code that was already fixed in the new commits. What's the most effective way to eliminate this redundant feedback while maintaining thorough analysis?|A. Include prior review findings in context, instructing Claude to only report new or still-unaddressed issues. | B. Add a post-processing filter that removes findings matching previous file paths and issue descriptions before posting comments. | C. Restrict the review scope to only files modified in the most recent push, excluding files from earlier commits. | D. Run reviews only on initial PR creation and final pre-merge state, skipping intermediate commits.|ANS:A|EXP:The exam guide's Task 3.6 skill list explicitly recommends including prior review findings in context on re-runs and instructing Claude to report only new or still-unaddressed issues. This leverages the model's natural-language reasoning about which earlier findings have been resolved by the new commits, rather than relying on brittle string matching or scope cuts that miss real issues.
Q16|D4|T1|4.3|Your exercise-logging agent supports two activity types: cardio (logged with time and distance) and strength (logged with reps and weight). The agent currently exposes a single `log_exercise` tool whose JSON schema declares parameters `activity_type`, `time_minutes`, `distance_km`, `reps`, and `weight_kg`, all marked optional with strict types. Production logs show that in roughly 18% of strength-training entries the agent populates `distance_km` (a phantom value alongside reps and weight), and ~9% of cardio entries are missing `time_minutes`. What change would most effectively eliminate these mis-typed metric pairings?|A. Update the system prompt to specify exactly which metrics belong to each activity type, with explicit examples for cardio and strength entries. | B. Replace `log_exercise` with two tools ? `log_cardio_session` (requires `time_minutes` and `distance_km`) and `log_strength_session` (requires `reps` and `weight_kg`) ? so the schema itself prevents mismatched metric pairings. | C. Have the tool's runtime handler validate the metric combination and return an error to the agent (causing it to retry with the same `log_exercise` tool) when cardio and strength fields are mixed. | D. Restructure the existing `log_exercise` input_schema to use `anyOf` requiring either (`time_minutes` + `distance_km`) or (`reps` + `weight_kg`), so the schema declares the conditional requirements while keeping a single tool.|ANS:B|EXP:The exam guide explicitly notes that strict JSON schemas via tool use eliminate syntax errors but do not prevent semantic errors like 'values in wrong fields.' A single tool with all-optional fields is a flat schema that the model can populate incorrectly even when types are strict. Splitting into two tools turns the activity-type choice into a tool-selection choice: the cardio tool literally cannot accept reps/weight, and the strength tool literally cannot accept distance/time. The discriminated-union pattern moves enforcement from prompt-level guidance (which the model can ignore) to schema-level structure (which the API call site rejects). This is the same 'programmatic enforcement at the wiring layer' principle as the get_customer prerequisite pattern in subdomain 1.5.
Q17|D3|T2|3.2|Your team has created a custom `/analyze-codebase` skill that performs deep exploration of large repositories and produces verbose output. After deployment, two problems have emerged: (1) the skill's lengthy output is cluttering the main conversation context, making it hard to continue working after the skill runs; (2) the skill occasionally uses destructive Bash operations like `git reset` because the developer forgot to restrict tool access when authoring the skill file. Where should you make changes to fix both issues?|A. Add a PostToolUse hook in settings.json that intercepts the skill's tool calls to filter its output and block destructive Bash commands after the skill completes. | B. Add instructions to the skill's prompt body telling Claude to avoid destructive operations and to produce a brief summary instead of the full exploration output. | C. In the skill's SKILL.md frontmatter, add `context: fork` to run the skill in an isolated sub-agent context (preventing output from polluting the main session) and `allowed-tools: [Read, Grep, Glob]` to restrict tool access to read-only operations, blocking Bash entirely. | D. Move the skill to `~/.claude/skills/` so it runs with the user's personal settings, which have destructive Bash commands disabled by default.|ANS:C|EXP:The exam guide covers both frontmatter fields directly: 'Skills in .claude/skills/ with SKILL.md files that support frontmatter configuration including context: fork, allowed-tools, and argument-hint.' Context: fork 'running skills in an isolated sub-agent context, preventing skill outputs from polluting the main conversation' ? this directly addresses problem (1). Configuring 'allowed-tools in skill frontmatter to restrict tool access during skill execution (e.g., limiting to file write operations to prevent destructive actions)' ? this directly addresses problem (2). Both problems are skill-level configuration concerns, and both have documented frontmatter solutions. Hooks (A, B) are the wrong layer for skill UX and behavior ? they intercept at the tool-execution level, not the skill-invocation level, and cannot implement context isolation.
Q18|D5|T1|5.4|You're adding error handling wrappers to external API calls across a 120-file codebase. The task has three phases: (1) discovering all API call locations and patterns, (2) designing the error handling approach collaboratively, and (3) implementing wrappers consistently. During Phase 1, Claude generates verbose output listing hundreds of call sites with context. Your context window is filling rapidly before you've finished discovery.
What's the most effective approach to complete this while maintaining implementation consistency?|A. Switch to headless mode with --continue, passing explicit context summaries between batch invocations to maintain continuity. | B. Continue all phases in the main conversation, using /compact periodically to reduce context usage as you progress through the files. | C. Define your error handling pattern in CLAUDE.md, then process files in batches across multiple sessions, relying on the shared memory file for consistency. | D. Use the Explore subagent for Phase 1 to isolate verbose output and return a summary, then continue Phases 2-3 in the main conversation.|ANS:D|EXP:The Explore subagent runs verbose discovery in its own isolated context and returns a compact summary to the parent, so the main conversation never absorbs the hundreds of raw call-site dumps. That preserves headroom in the main session for the collaborative design and consistent multi-file implementation phases.
Q19|D4|T1|4.2|Production metrics show your agent averages 4+ API round-trips per resolution. Analysis reveals Claude frequently requests get_customer and lookup_order in separate sequential turns even when both are needed upfront. What's the most effective way to reduce round-trips?|A. Implement speculative execution that automatically calls likely-needed tools alongside any requested tool, returning all results regardless of what was requested. | B. Increase max_tokens to give Claude more space to plan ahead and naturally batch its tool requests. | C. Create composite tools like get_customer_with_orders that bundle common lookup combinations into single calls. | D. Prompt Claude to batch tool requests per turn, and return all tool results together before the next API call.|ANS:D|EXP:The documented technique for cutting round-trips is to have Claude emit multiple independent tool calls in a single assistant turn so the harness can run them concurrently and return all results before the next API call. Prompting Claude to batch get_customer and lookup_order in one turn collapses two sequential round-trips into one.
Q20|D4|T1|4.1|Your customer-support agent's system prompt grew from 600 tokens at launch to 3,400 tokens after six months as the team added 'don't say X', 'always say Y', and 'remember to consider Z' rules. The prompt has roughly 40 such rules now spread across paragraphs by no particular organization. Quality review finds two failure patterns: (1) the agent forgets earlier rules later in a conversation, especially after the 8th turn, and (2) when two rules apply to the same situation, the agent applies whichever was mentioned more recently in the prompt rather than the more important one. What is the right restructuring approach?|A. Convert the rules into a longer, more emphatic flat list with each rule prefixed by a severity label ('CRITICAL: ...', 'IMPORTANT: ...') and a numbered index so the agent can reason about precedence by number. | B. Decompose the prompt into a small set of role/goal/criteria/examples sections ? a concise role statement, the agent's goals, explicit decision criteria for the most common conflict cases, and 3-5 worked examples showing the criteria applied. Move rare edge-case rules out of the system prompt into runtime tool descriptions or contextual injection. | C. Keep the 40 rules but ask the agent to re-read the system prompt every 5 turns by injecting a `<reminder>system prompt: ...</reminder>` block back into the conversation. | D. Increase the model's context window allocation for the system prompt by using prompt caching with a long TTL on the full 3,400-token prompt, so the rules are always in-context cheaply and the agent doesn't 'forget' them.|ANS:B|EXP:The exam guide's pattern for long, drift-prone system prompts is decomposition: clearly named sections (role, goal, criteria, examples) with conflict-resolution criteria explicit at the section level, and 3-5 worked examples that anchor the agent's behavior. This is the same restructuring that resolves the long-prompt drift case in q-e83155ef (2,800 tokens + 12-turn drift ? few-shot examples). Both failure patterns in the stem (forgetting late-prompt rules, applying recency over importance) come from a flat 40-rule list with no structure for the agent to use as a stable mental model. Decomposition + worked examples gives the agent a checkable shape.
Q21|D1|T1|1.4|A customer-onboarding agent runs a four-step workflow: (1) collect_intake, (2) verify_identity, (3) provision_accounts, (4) send_welcome_email. The agent uses one tool per step, each independently described in the system prompt. Audit of a recent week shows ~6% of cases where send_welcome_email fired BEFORE verify_identity completed (the agent skipped step 2 because the customer's intake form looked complete) ? these welcome emails went to unverified accounts and several reached fraudulent sign-ups. What is the right structural fix to enforce the step order?|A. Reorder and re-bolden the system prompt to make the four-step sequence visually prominent ? number each step, add explicit 'do NOT skip step 2' instructions, and include a positive example of the full sequence executed correctly. | B. Make each tool's host implementation programmatically check the workflow state: verify_identity refuses to run unless collect_intake has stored an intake record; provision_accounts refuses unless verify_identity has stored a verified-identity record; send_welcome_email refuses unless provision_accounts has stored a provisioned-accounts record. | C. Combine the four tools into a single `run_onboarding` tool that internally calls collect_intake, verify_identity, provision_accounts, and send_welcome_email in order, so the agent only chooses to onboard or not ? eliminating the step-selection decision entirely. | D. Add a post-execution audit hook that scans completed workflows nightly and flags cases where send_welcome_email fired without a verify_identity record, then re-issues a verification request and a corrected welcome email for those cases.|ANS:B|EXP:Multi-step workflows with consequential side effects (sending real emails, provisioning real accounts) need programmatic prerequisite enforcement at the tool layer. Each tool checks its preconditions against persistent workflow state and refuses with a clear error if those preconditions aren't met. The agent's prompt-level sequencing is best-effort; the tool layer's prerequisite check is deterministic. This is the same principle behind the q-bb370403 get_customer-prerequisite pattern (4.3) and the q-1e1698c2 tool-interception-hook pattern (1.5): high-stakes side effects gate on host-enforced preconditions, not on agent compliance.
Q22|D2|T3|2.5|A developer-productivity agent searches a 200-file Python codebase for all callers of `process_payment`. The agent currently uses a Bash tool to run `find . -name '*.py' -exec grep -l 'process_payment' {} \;`. This takes ~12 seconds and returns false positives from commented-out code. What is the correct tool selection per the exam guide's documented patterns for built-in tool use?|A. Add an LSP-backed semantic search MCP server to find all callers with full type resolution, eliminating false positives at the source. | B. Use the built-in Grep tool with the pattern `process_payment` and a `*.py` file glob ? it is purpose-built for content search across a codebase. | C. Continue using the Bash tool but switch from `find+grep` to `rg process_payment --glob '*.py'` (ripgrep) to improve speed. | D. Use the Glob tool with pattern `**/*.py` to list all Python files, then call Read on each result and parse for the function name.|ANS:B|EXP:The exam guide assigns Grep a specific role: 'Grep for content search (searching file contents for patterns like function names, error messages, or import statements).' The skills section adds: 'Selecting Grep for searching code content across a codebase (e.g., finding all callers of a function).' This is exactly the scenario ? finding all callers of `process_payment` across Python files. Using the built-in Grep tool instead of Bash+grep aligns with the documented tool-selection model. Option A (LSP-backed semantic MCP server) is a sophistication trap: the exam guide specifies choosing existing community MCP servers over custom implementations for standard integrations, reserving custom servers for team-specific workflows; an LSP semantic search server for a pattern-match use case is a custom implementation for a standard task. Option C improves the wrong tool: switching Bash from find+grep to ripgrep makes the wrong choice faster but does not align with the documented built-in tool model. Option D conflates Glob (file path patterns) with Grep (file content search): Glob finds files by name pattern, not by content; using Glob then Read is a multi-step workaround for what Grep does in one call. Note: the evaluation axis is documented tool-role alignment ? whether Grep or Bash is the right tool for content search ? not which option produces fewer false positives on commented code (Grep also matches comments by default).
Q23|D3|T2|3.6|Your CI pipeline includes two Claude-powered code review modes: a pre-merge-commit hook that blocks PR merging until complete, and "deep analysis" that runs overnight, polls for batch completion, then posts detailed suggestions to the PR. You want to reduce API costs using the Message Batches API, which offers 50% cost savings but requires polling and may take up to 24 hours to complete. Which mode should use batch processing?|A. Both modes | B. Neither mode | C. Deep analysis only | D. Pre-merge-commit hook only|ANS:C|EXP:The Message Batches API trades latency (up to 24 hours) for ~50% cost savings. It fits non-blocking, latency-tolerant workloads like the overnight 'deep analysis' job that already polls for completion. It is inappropriate for the pre-merge-commit hook, which blocks PR merging and requires synchronous, low-latency responses.
Q24|D2|T2|2.1|A developer productivity agent has two tools: `search_codebase` (description: 'Search and analyze code') and `search_docs` (description: 'Search and analyze documentation'). Production logs show the agent routes documentation queries to `search_codebase` approximately 30% of the time, and code queries to `search_docs` in about 15% of cases. The development team has tried adding a system prompt instruction: 'Always call search_codebase for code questions and search_docs for documentation questions.' The misrouting rate has not improved. What is the most effective next step?|A. Implement a pre-processing routing layer that classifies the query type (code vs. documentation) before invoking the agent, and pass only the appropriate tool in each request. | B. Expand each tool's description to clearly differentiate purpose, expected inputs, what it searches, and when to use it instead of the other ? for example: `search_codebase`: 'Searches source files (.py, .js, .ts), function bodies, class definitions, and test files. Use for: finding function implementations, tracing call chains, locating bug sources. NOT for: API reference, README files, or architecture docs.' | C. Consolidate `search_codebase` and `search_docs` into a single `search` tool with a `target` enum parameter (`code` or `documentation`) so the model makes one tool call and the backend routes internally. | D. Add few-shot examples to the system prompt showing the agent correctly routing 5 representative queries to each tool, so the model can pattern-match new queries to the correct tool.|ANS:B|EXP:The exam guide identifies this exact failure pattern: 'How ambiguous or overlapping tool descriptions cause misrouting (e.g., analyze_content vs analyze_document with near-identical descriptions).' The fix is also explicit: 'Writing tool descriptions that clearly differentiate each tool's purpose, expected inputs, outputs, and when to use it versus similar alternatives.' The system prompt instruction has already been tried and failed ? it operates at the wrong layer. Tool descriptions are the primary mechanism the model uses for selection; near-identical descriptions create genuine ambiguity that instructions cannot reliably overcome. Option B directly addresses the root cause with the exam guide's recommended technique. Option A (routing layer) is an over-engineering of a problem with a lower-effort fix available. Option C (consolidation) changes the tool architecture rather than fixing the descriptions. Option D (few-shot) improves behavior marginally but doesn't fix the underlying ambiguous descriptions.
Q25|D1|T1|1.5|You are integrating Claude Code into your CI/CD pipeline. The system runs automated code reviews, generates test cases, and provides feedback on pull requests. You need to design prompts that provide actionable feedback and minimize false positives.

Your CI review uses tool_use to produce a JSON finding object with required fields file_path, start_line, severity, owner_team, linked_ticket, and suggested_fix. In PRs that lack ownership or ticket metadata, reviews contain plausible but nonexistent owner teams and tickets, which downstream automation routes incorrectly. What schema change best addresses this while preserving useful findings?|A. Reject any finding missing routing metadata before posting comments, ensuring only fully populated findings reach developers. | B. Keep all fields required and add stronger prompt wording telling Claude never to invent values from context. | C. Infer absent metadata from repository naming conventions so every finding contains complete routing information for automation. | D. Redesign the schema so unavailable source-dependent fields are nullable or optional, with instructions to return null unless directly evidenced.|ANS:D|EXP:Making source-dependent fields like owner_team and linked_ticket nullable or optional, with instructions to return null unless directly evidenced, eliminates hallucinated routing metadata without discarding the finding itself. The exam guide explicitly identifies this as the schema design solution: optional/nullable fields prevent fabrication while preserving valid findings that do have evidence.
Q26|D5|T1|5.4|A developer using Claude Code on a 600-file Django monorepo notices that after ~3 hours of exploration, the agent's answers about specific classes (e.g., `OrderRefundProcessor`) have started referencing "typical patterns" or "a base class" rather than the actual class definition the agent analyzed earlier. The `/compact` command has fired twice during the session. What is the right structural change to preserve the agent's grounded knowledge across continued exploration of the codebase?|A. Switch to a model variant with the largest available context window (200k tokens) so `/compact` fires less frequently and earlier context survives longer in the session. | B. Have the main agent spawn focused subagents for each new component investigation, with each subagent returning a concise summary to the main agent ? and maintain a scratchpad file on disk recording key findings so the main agent's context stays lean and durable knowledge survives compaction. | C. Disable `/compact` entirely so context is never lost, and increase the per-turn output token limit to compensate for the longer accumulated prompts. | D. After each exploration phase, ask the agent to summarize what it learned in its final response, then have the user copy-paste the summary back as the opening of their next message before continuing to the next question.|ANS:B|EXP:The exam guide for subdomain 5.4 names exactly this pattern: subagents isolate verbose exploration output while the main agent coordinates high-level understanding, and scratchpad files persist key findings across context boundaries. The symptom in the stem ('typical patterns' instead of specific class names) is the documented signature of context degradation in extended sessions. The fix is structural ? partition exploration across subagents and persist durable findings on disk ? not to scale up a single agent's context.
Q27|D5|T1|5.2|Over several turns discussing investment strategy, a user stated "I have a very low risk tolerance" and later "I want to maximize my returns." They now ask: "What should I invest in?" Which approach best ensures the recommendation aligns with the user's actual priority?|A. Proceed with the most recently stated preference. | B. Provide separate recommendations for both scenarios. | C. Recommend a balanced portfolio without addressing the conflict. | D. Surface the contradiction and ask the user to clarify which matters more.|ANS:D|EXP:When the user has stated two conflicting priorities, neither defaulting to the latest, hedging, nor compromising preserves their actual intent. The right pattern is to surface the contradiction explicitly and ask which constraint matters more, the same clarification-on-ambiguity principle that applies to multiple tool matches.
Q28|D2|T3|2.4|Your team is setting up a shared GitHub MCP server for all engineers on a project. The server requires a GitHub API token for authentication. You want every engineer who clones the repository to have the MCP server available without each person having to manually configure it, and you want to avoid committing the actual token to version control. What is the correct configuration approach?|A. Add the MCP server configuration to the root `CLAUDE.md` file under a `[mcp_servers]` section. | B. Add the MCP server to `~/.claude.json` on each developer's machine with their personal token hardcoded. | C. Create a setup script that each developer runs once to add the MCP server to their personal `~/.claude.json` with their token. | D. Add the MCP server to the project-scoped `.mcp.json` file with the token specified using environment variable expansion (e.g., `${GITHUB_TOKEN}`), and commit `.mcp.json` to the repository.|ANS:D|EXP:Domain 2.4 prescribes configuring shared MCP servers in project-scoped .mcp.json with environment variable expansion (e.g., ${GITHUB_TOKEN}) for authentication tokens. Committing .mcp.json gives every developer the server definition automatically; using ${GITHUB_TOKEN} keeps the actual secret in each developer's environment, so no token is checked in.
Q29|D5|T1|5.4|A pull request modifies 14 files across the stock tracking module. Your single-pass review analyzing all files together produces inconsistent results: detailed feedback for some files but superficial comments for others, obvious bugs missed, and contradictory feedback-flagging a pattern as problematic in one file while approving identical code elsewhere in the same PR. How should you restructure the review?|A. Run three independent review passes on the full PR and only flag issues that appear in at least two of the three runs. | B. Require developers to split large PRs into smaller submissions of 3-4 files before the automated review runs. | C. Split into focused passes: analyze each file individually for local issues, then run a separate integration-focused pass examining cross-file data flow. | D. Switch to a higher-tier model with a larger context window to give all 14 files adequate attention in one pass.|ANS:C|EXP:Single-pass review across many files dilutes attention, producing inconsistent depth and contradictory feedback. The documented remedy is to split the work: per-file passes for local issues plus a separate integration-focused pass for cross-file data flow. This preserves focus per file and dedicates a distinct pass to system-level concerns.
Q30|D5|T1|5.4|Your CI/CD system performs three types of Claude-powered analysis: (1) quick style checks on each PR that block merging until complete, (2) comprehensive security audits of the entire codebase run weekly, and (3) test case generation triggered nightly for recently-modified modules. The Message Batches API offers 50% cost savings but can take up to 24 hours to process. You want to optimize API costs while maintaining acceptable developer experience. Which combination correctly matches each task to its API approach?|A. Use synchronous calls for PR style checks; use the Message Batches API for weekly security audits and nightly test generation. | B. Use synchronous calls for PR style checks and nightly test generation; use Message Batches API only for weekly security audits. | C. Use synchronous calls for all three tasks for consistent response times, and rely on prompt caching to reduce costs across all workloads. | D. Use the Message Batches API for all three tasks to maximize the 50% cost savings, and configure the pipeline to poll for batch completion.|ANS:A|EXP:PR style checks block merging and need synchronous responses; latency-tolerant work like weekly security audits and nightly test generation is exactly the workload pattern the Batch API targets at 50% off. Match latency requirements to the API mode: synchronous for blocking pre-merge checks, batch for the rest.